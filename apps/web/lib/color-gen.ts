import { argbFromHex, rgbaFromArgb, Scheme, themeFromSourceColor } from "@material/material-color-utilities";

// Simple demonstration of HCT.
// const color: {
//   light: Scheme;
//   dark: Scheme;
// } = themeFromSourceColor(argbFromHex("#3D86FF")).schemes;

// const light = color.light.toJSON();
// const dark = color.dark.toJSON();

// [...Object.keys(dark)].map((e) => {
//   const { r, g, b } = rgbaFromArgb(dark[e as keyof typeof dark]);
//   console.log("dark: ", e, `rgb(${r}, ${g}, ${b})`);
// });

// [...Object.keys(light)].map((e) => {
//   const { r, g, b } = rgbaFromArgb(light[e as keyof typeof light]);
//   console.log("light: ", e, `rgb(${r}, ${g}, ${b})`);
// });

console.log(process.cwd())


// Simple demonstration of HCT.
// const color = Hct.fromInt(0xff4285f4);
// console.log(`Hue: ${color.hue}`);
// console.log(`Chrome: ${color.chroma}`);
// console.log(`Tone: ${color.tone}`);
// console.log(`Hue: ${color.hue}`);
// console.log(`Chrome: ${color.chroma}`);
// console.log(`Tone: ${color.tone}`);
